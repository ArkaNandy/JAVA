package Stackdatastructure;

public class CustomQueue {
    private int[] array;
    private static int  DEFAULT_SIZE=10;
    int endpointer=-1; // in queue we can insert from the last as well

    public CustomQueue(){
        this(DEFAULT_SIZE);

    }










    
    
}
