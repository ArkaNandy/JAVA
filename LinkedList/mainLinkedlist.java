package LinkedList;

import java.util.LinkedList;

import java.util.*;
public class mainLinkedlist{



   
    public static void main(String[] args) {

        LinkedList list= new LinkedList<>(); // collection api has got a class called linked list of which which we can create an object of and perform certain operations

        list.insert(5);
        list.insert(45);
        list.insert(12)
       // list.size();
//
        
    }

}